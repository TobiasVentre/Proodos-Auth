{
  "name": "@proodos/infrastructure",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "@proodos/application": "1.0.0",
    "@proodos/domain": "1.0.0",
    "dotenv": "^17.2.3",
    "ldapjs": "^3.0.7",
    "sequelize": "^6.37.7",
    "tedious": "^19.1.3"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/ldapjs": "^2.2.5",
    "sequelize-cli": "^6.6.2"
  },
  "scripts": {
    "build": "tsc -p .",
    "db:migrate": "npm run build && sequelize-cli db:migrate",
    "db:migrate:undo": "npm run build && sequelize-cli db:migrate:undo",
    "db:seed": "npm run build && sequelize-cli db:seed:all",
    "db:seed:undo": "npm run build && sequelize-cli db:seed:undo:all"
  }
}
